# credentials.yml
# no `action_endpoint` here!

rest:
#  optional CORS settings (if you need them)
#  cors: "*"

# if you ever want socket.io:
# socketio:
#   user_message_evt: user_uttered
#   bot_message_evt: bot_uttered
